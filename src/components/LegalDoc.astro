---
interface Section {
  heading: string;
  body: string;
}

interface Props {
  kicker: string;
  title: string;
  lastUpdated: string;
  contactEmail: string;
  contactLabel: string;
  sections: Section[];
}

const { kicker, title, lastUpdated, contactEmail, contactLabel, sections } = Astro.props;
---

<article class="legal-page">
  <header class="legal-hero">
    <p>{kicker}</p>
    <h1>{title}</h1>
    <span>{lastUpdated}</span>
  </header>

  <div class="legal-body">
    <div class="legal-index" aria-hidden="true">
      <span>Document</span>
      <strong>{String(sections.length).padStart(2, '0')} sections</strong>
    </div>

    <div class="legal-sections">
      {sections.map((section, index) => (
        <section id={`s-${index + 1}`} class="legal-section reveal" data-reveal>
          <span>{String(index + 1).padStart(2, '0')}</span>
          <div>
            <h2>{section.heading.replace(/^\d+\.\s*/, '')}</h2>
            <p>{section.body}</p>
          </div>
        </section>
      ))}
    </div>
  </div>

  <section class="legal-contact">
    <p>{contactLabel}</p>
    <a href={`mailto:${contactEmail}`}>{contactEmail}</a>
  </section>
</article>

<script>
  const sections = document.querySelectorAll<HTMLElement>('.legal-page [data-reveal]');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add('is-visible');
      observer.unobserve(entry.target);
    });
  }, { threshold: 0.12 });
  sections.forEach((section) => observer.observe(section));
</script>

<style>
  .legal-page {
    --ink: #07111f;
    --night: #061426;
    --paper: #efede7;
    --line: rgba(7, 17, 31, 0.17);
    color: var(--ink);
    background: var(--paper);
  }

  .legal-hero {
    min-height: 26rem;
    padding: 6rem 6.7vw 4rem;
    background: var(--night);
    color: #f4f2ec;
  }

  .legal-hero p,
  .legal-hero span {
    margin: 0;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    color: rgba(226, 234, 242, 0.62);
  }

  .legal-hero h1 {
    max-width: 64rem;
    margin: 3.5rem 0 3rem;
    font-size: 4.8rem;
    font-weight: 650;
    line-height: 1.08;
    letter-spacing: 0;
  }

  .legal-body {
    display: grid;
    grid-template-columns: 15rem minmax(0, 1fr);
    gap: 4rem;
    padding: 7rem 6.7vw 9rem;
  }

  .legal-index {
    position: sticky;
    top: 8rem;
    align-self: start;
  }

  .legal-index span,
  .legal-index strong {
    display: block;
    font-size: 0.75rem;
  }

  .legal-index span {
    margin-bottom: 0.7rem;
    color: #707070;
    text-transform: uppercase;
  }

  .legal-section {
    display: grid;
    grid-template-columns: 5rem minmax(0, 1fr);
    gap: 2rem;
    padding: 0 0 4rem;
    margin-bottom: 4rem;
    border-bottom: 1px solid var(--line);
  }

  .legal-section > span {
    color: #707070;
    font-size: 0.78rem;
  }

  .legal-section h2 {
    margin: 0 0 1.5rem;
    font-size: 2rem;
    font-weight: 600;
    line-height: 1.25;
  }

  .legal-section p {
    margin: 0;
    color: #444444;
    font-size: 0.98rem;
    line-height: 1.85;
    white-space: pre-line;
  }

  .legal-contact {
    padding: 6rem 6.7vw;
    background: var(--night);
    color: #ffffff;
  }

  .legal-contact p {
    margin: 0 0 2rem;
    color: #a8a8a8;
    font-size: 0.75rem;
    text-transform: uppercase;
  }

  .legal-contact a {
    display: inline-block;
    max-width: 100%;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid currentColor;
    font-size: 2.2rem;
    font-weight: 600;
    overflow-wrap: anywhere;
  }

  @media (max-width: 760px) {
    .legal-hero {
      min-height: 22rem;
      padding: 4.5rem 1.25rem 3rem;
    }

    .legal-hero h1 {
      margin: 3rem 0 2.5rem;
      font-size: 3rem;
    }

    .legal-body {
      grid-template-columns: 1fr;
      gap: 3rem;
      padding: 4.5rem 1.25rem 5rem;
    }

    .legal-index {
      position: static;
    }

    .legal-section {
      grid-template-columns: 2.5rem minmax(0, 1fr);
      gap: 1rem;
      padding-bottom: 3rem;
      margin-bottom: 3rem;
    }

    .legal-section h2 {
      font-size: 1.55rem;
    }

    .legal-contact {
      padding: 4.5rem 1.25rem;
    }

    .legal-contact a {
      font-size: 1.3rem;
    }
  }
</style>
