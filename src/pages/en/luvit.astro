---
import Layout from '../../layouts/Layout.astro';

const accent = '#FF6B9D';
const deep = '#4B2E83';
const yellow = '#FFC850';

const appStoreUrl = 'https://apps.apple.com/kr/app/%EB%9F%AC%EB%B9%97-luvit/id6762438141';

const heroSubtitle = 'A tiny universe.\nOne mission a day.';

const features = [
  {
    title: 'Every 9 PM',
    body: 'The little spaceship wakes up. A mission card lands between the two of you. Carry it out in secret while your partner tries to guess yours.',
  },
  {
    title: 'Your own room',
    body: 'One spaceship, two rabbits, and a midnight sky that changes every day. A small space that belongs only to you and them.',
  },
  {
    title: 'Midnight reveal',
    body: 'When the day closes, missions get graded and guesses unlock. Success means points, failure means a tiny cosmic forfeit. Survive until 9 PM tomorrow.',
  },
];

const steps = [
  {
    title: 'Into your universe',
    body: 'Google or Apple in 10 seconds. A single line for a nickname.',
    image: '/screenshots/luvit/01-login.png',
  },
  {
    title: 'Six-digit invite',
    body: 'Share your code with them. The moment two of you board the same spaceship.',
    image: '/screenshots/luvit/02-connect.png',
  },
  {
    title: 'Today\'s mission lands',
    body: '9 PM. Speech bubble on the rabbit beside you. Same room, different missions, different secrets.',
    image: '/screenshots/luvit/03-mission.png',
  },
  {
    title: 'Guess theirs',
    body: 'Tap the other rabbit, pick from four. Right answer = +100. Wrong = one more clue.',
    image: '/screenshots/luvit/04-judge.png',
  },
  {
    title: 'Midnight, results',
    body: 'Stamp the day if you both made it. A small forfeit if not. Survive until 9 PM tomorrow.',
    image: '/screenshots/luvit/05-result.png',
  },
];

const faq = [
  {
    q: 'Do we have to be a couple?',
    a: 'Close friends or family works too. But the mission pool has things like "hug each other today," so Luvit feels most natural for couples.',
  },
  {
    q: 'Who writes the missions?',
    a: 'East Noise refreshes the pool weekly — nothing too heavy, mostly 5-minute daily actions. Custom missions are planned.',
  },
  {
    q: 'Any ads?',
    a: 'No ads. Free. Future plans include small IAP for room decor and themed mission packs.',
  },
];

const moments = [
  '6-day streak unlocks the "Streak Cosmonaut" badge',
  'The last push at 11:59 PM',
  'When they guess yours in one second',
  'Both succeeded → two rabbits float hearts',
  'A cute forfeit on the day you forgot',
  'New furniture morning in your room',
];

const promises = [
  { x: 'Zero ads', y: 'Never. We won\'t even leave space for them.' },
  { x: 'Just the two of you', y: 'No friend invites, no rankings, no other couples.' },
  { x: 'One push a day', y: 'One at 9 PM. One at midnight. That\'s it.' },
  { x: 'Minimal data', y: 'Just a nickname and your rabbit. No photos, contacts, or socials.' },
];
---

<Layout locale="en" title="Luvit — A tiny universe, one mission a day." description="Every 9 PM, one mission lands in your couple's spaceship. Secret play, midnight reveal. On the App Store." path="/en/luvit">

  <!-- Hero -->
  <section class="bg-luvit-ambient relative overflow-hidden">
    <div class="pointer-events-none absolute inset-0 -z-0">
      <span class="petal-fall-1 absolute" style={`left: 7%; top: 0; animation-delay: 0s;`}>
        <svg width="14" height="14" viewBox="0 0 20 20"><path d="M10 17s-6-4.5-6-9a4 4 0 0 1 7-2.6A4 4 0 0 1 16 8c0 4.5-6 9-6 9z" fill={accent} opacity="0.7"/></svg>
      </span>
      <span class="petal-fall-2 absolute" style="left: 22%; top: 0; animation-delay: 3s;">
        <svg width="10" height="10" viewBox="0 0 20 20"><circle cx="10" cy="10" r="3" fill={yellow} opacity="0.85"/></svg>
      </span>
      <span class="petal-fall-3 absolute" style="left: 38%; top: 0; animation-delay: 1.4s;">
        <svg width="13" height="13" viewBox="0 0 20 20"><path d="M10 17s-6-4.5-6-9a4 4 0 0 1 7-2.6A4 4 0 0 1 16 8c0 4.5-6 9-6 9z" fill={deep} opacity="0.65"/></svg>
      </span>
      <span class="petal-fall-1 absolute" style="left: 56%; top: 0; animation-delay: 7s;">
        <svg width="9" height="9" viewBox="0 0 20 20"><circle cx="10" cy="10" r="3" fill={yellow} opacity="0.9"/></svg>
      </span>
      <span class="petal-fall-2 absolute" style="left: 72%; top: 0; animation-delay: 2.2s;">
        <svg width="14" height="14" viewBox="0 0 20 20"><path d="M10 17s-6-4.5-6-9a4 4 0 0 1 7-2.6A4 4 0 0 1 16 8c0 4.5-6 9-6 9z" fill={accent} opacity="0.8"/></svg>
      </span>
      <span class="petal-fall-3 absolute" style="left: 86%; top: 0; animation-delay: 5s;">
        <svg width="11" height="11" viewBox="0 0 20 20"><circle cx="10" cy="10" r="3" fill={accent} opacity="0.7"/></svg>
      </span>
      <span class="petal-fall-1 absolute" style="left: 93%; top: 0; animation-delay: 11s;">
        <svg width="9" height="9" viewBox="0 0 20 20"><path d="M10 17s-6-4.5-6-9a4 4 0 0 1 7-2.6A4 4 0 0 1 16 8c0 4.5-6 9-6 9z" fill={deep} opacity="0.75"/></svg>
      </span>
    </div>

    <div class="relative z-10 max-w-5xl mx-auto w-full px-6 pt-16 md:pt-28 pb-20 grid grid-cols-1 md:grid-cols-[1.2fr_1fr] gap-10 md:gap-16 items-center">
      <div>
        <div class="flex items-center gap-2 mb-6 reveal">
          <span class="inline-block w-2 h-2 rounded-full pulse-dot" style={`background-color: ${accent}`}></span>
          <p class="text-xs uppercase tracking-[0.18em] text-[--color-muted] font-medium">
            Indie App · East Noise
          </p>
        </div>
        <h1 class="text-5xl md:text-7xl font-black tracking-tight leading-[1.05] mb-6 reveal" data-delay="100">
          Luvit<span style={`color: ${accent}`}>.</span>
        </h1>
        <p class="text-2xl md:text-4xl font-black tracking-tight leading-[1.15] mb-8 whitespace-pre-line reveal" data-delay="200">
          {heroSubtitle}
        </p>
        <p class="text-base md:text-lg text-[--color-muted] max-w-2xl leading-relaxed mb-10 reveal" data-delay="300">
          Every 9 PM, a mission card lands in your couple's spaceship. Carry it out in secret while your partner guesses yours. Midnight reveals everything. Two rabbits live in a room that belongs only to the two of you.
        </p>
        <div class="flex flex-wrap items-center gap-3 reveal" data-delay="400">
          <a href={appStoreUrl} target="_blank" rel="noopener" class="magnet-cta inline-flex items-center transition-transform hover:scale-105" aria-label="Download on the App Store">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 170 50" class="h-12 md:h-14 w-auto" role="img" aria-hidden="true">
              <rect width="170" height="50" rx="9" fill="#000"/>
              <path d="M30.5 26.6c0-3 2.5-4.5 2.6-4.5-1.4-2-3.6-2.3-4.4-2.3-1.9-.2-3.6 1.1-4.6 1.1-1 0-2.4-1.1-4-1-2 0-3.9 1.2-5 3-2.1 3.7-.5 9.1 1.5 12.1 1 1.5 2.2 3.1 3.8 3 1.5-.1 2.1-1 3.9-1 1.8 0 2.4 1 4 1 1.7 0 2.7-1.5 3.7-2.9 1.2-1.7 1.7-3.3 1.7-3.4-.1-.1-3.2-1.2-3.2-4.9zm-3-9c.8-1 1.4-2.4 1.3-3.8-1.2.1-2.7.8-3.6 1.8-.8.9-1.5 2.4-1.3 3.7 1.4.1 2.8-.7 3.6-1.7z" fill="#fff"/>
              <text x="46" y="20" fill="#fff" font-size="8" font-family="-apple-system, BlinkMacSystemFont, Helvetica, sans-serif" letter-spacing="0.3">Download on the</text>
              <text x="46" y="38" fill="#fff" font-size="17" font-weight="600" font-family="-apple-system, BlinkMacSystemFont, Helvetica, sans-serif" letter-spacing="-0.4">App Store</text>
            </svg>
          </a>
          <a href="#story" class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-[--color-border] bg-white text-sm font-semibold hover:border-[--color-fg] transition-colors min-h-[44px]">
            See how it works ↓
          </a>
        </div>
      </div>

      <div class="reveal flex justify-center md:justify-end" data-delay="200">
        <div id="hero-tilt" class="tilt-3d relative w-full max-w-md">
          <div class="absolute inset-0 -z-10" style={`background: radial-gradient(circle at 50% 50%, ${accent}1A, transparent 65%); filter: blur(20px);`}></div>

          <div class="relative flex items-end justify-center gap-2 md:gap-6">
            <div class="relative bunny bunny-left">
              <img src="/screenshots/luvit/rabbit_blue.svg" alt="Blue rabbit — you" class="w-32 md:w-40 drop-shadow-xl" loading="eager"/>
              <div class="absolute -top-12 -left-2 md:-left-6 bg-white rounded-2xl border border-[--color-border] px-3 py-2 text-xs md:text-sm font-bold whitespace-nowrap bubble-pop" style={`color: ${deep};`}>
                <span>What's my mission?</span>
                <span class="absolute -bottom-1 left-6 w-2 h-2 bg-white border-r border-b border-[--color-border] transform rotate-45"></span>
              </div>
            </div>
            <div class="relative bunny bunny-right">
              <img src="/screenshots/luvit/rabbit_pink.svg" alt="Pink rabbit — partner" class="w-32 md:w-40 drop-shadow-xl" loading="eager"/>
              <div class="absolute -top-12 -right-2 md:-right-6 px-3 py-2 rounded-2xl text-xs md:text-sm font-bold whitespace-nowrap text-white bubble-pop" style={`background-color: ${accent}; animation-delay: 0.3s;`}>
                <span>Guess mine!</span>
                <span class="absolute -bottom-1 right-6 w-2 h-2 transform rotate-45" style={`background-color: ${accent};`}></span>
              </div>
            </div>
          </div>

          <span class="absolute float-1 text-lg" style="top: -8%; left: 10%;">💕</span>
          <span class="absolute float-2 text-base" style="top: 30%; right: 0%;">✨</span>
          <span class="absolute float-3 text-sm" style="bottom: -5%; left: 5%;">⭐</span>
          <span class="absolute float-1 text-base" style="top: 10%; right: 15%; animation-delay: 1.5s;">💫</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Promises -->
  <section class="max-w-5xl mx-auto w-full px-6 py-20 border-t border-[--color-border]">
    <p class="text-xs uppercase tracking-[0.18em] text-[--color-muted] font-medium mb-3 reveal">
      The Luvit promise
    </p>
    <h2 class="text-2xl md:text-4xl font-black tracking-tight leading-tight mb-12 reveal" data-delay="100">
      How is it different from<br/>
      <span style={`color: ${accent}`}>other couple apps?</span>
    </h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-px bg-[--color-border] border border-[--color-border]">
      {promises.map((p, i) => (
        <article class="bg-white p-7 reveal" data-delay={String((i + 1) * 80)}>
          <div class="text-xl md:text-2xl font-black tracking-tight mb-2" style={`color: ${accent}`}>
            {p.x}
          </div>
          <p class="text-sm text-[--color-muted] leading-relaxed">
            {p.y}
          </p>
        </article>
      ))}
    </div>
  </section>

  <!-- Features -->
  <section class="max-w-5xl mx-auto w-full px-6 py-20 border-t border-[--color-border]">
    <h2 class="text-2xl md:text-4xl font-black tracking-tight leading-tight mb-12 reveal">
      Once a day,<br/>
      <span style={`color: ${accent}`}>your tiny universe</span><br/>
      <span class="text-[--color-muted]">wakes up.</span>
    </h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      {features.map((f, i) => (
        <article class="lift-luvit reveal bg-white p-8 rounded-2xl border border-[--color-border]" data-delay={String((i + 1) * 100)}>
          <div class="text-3xl font-black tracking-tight mb-4" style={`color: ${accent}`}>
            {String(i + 1).padStart(2, '0')}
          </div>
          <h3 class="text-lg font-bold tracking-tight mb-3">
            {f.title}
          </h3>
          <p class="text-sm text-[--color-muted] leading-relaxed">
            {f.body}
          </p>
        </article>
      ))}
    </div>
  </section>

  <!-- Sticky scroll storytelling -->
  <section class="border-t border-[--color-border] bg-gradient-to-b from-[--color-bg] to-white">
    <div id="story" class="relative" style="height: 500vh;">
      <div class="sticky top-0 h-screen flex items-center overflow-hidden">
        <div class="max-w-5xl mx-auto w-full px-6 grid grid-cols-1 md:grid-cols-2 gap-10 items-center">
          <div class="relative aspect-square w-full max-w-md mx-auto md:mx-0">
            <div class="absolute inset-0 rounded-full" style={`background: radial-gradient(circle, ${accent}1F, transparent 70%);`}></div>

            <div class="story-stage story-active absolute inset-0 flex items-center justify-center" data-stage="0">
              <div class="relative w-3/5">
                <div class="bg-white rounded-2xl border border-neutral-200 p-6 shadow-lg">
                  <div class="text-xs font-bold tracking-[0.18em] mb-3" style={`color: ${accent}`}>Today · 21:00</div>
                  <div class="text-xl md:text-2xl font-black tracking-tight leading-snug">
                    Hold hands<br/>before they do.
                  </div>
                  <div class="mt-5 inline-flex items-center gap-1 px-3 py-1.5 rounded-full text-xs font-bold text-white" style={`background-color: ${accent}`}>
                    Accept →
                  </div>
                </div>
              </div>
            </div>

            <div class="story-stage story-hidden absolute inset-0 flex items-center justify-center" data-stage="1">
              <div class="relative w-3/5">
                <div class="flex items-center justify-around mb-4">
                  <div class="text-5xl">🐰</div>
                  <div class="text-5xl" style="filter: hue-rotate(280deg);">🐰</div>
                </div>
                <div class="bg-white rounded-xl border border-neutral-200 p-3 text-center">
                  <div class="text-xs font-bold mb-1" style={`color: ${deep}`}>On a secret mission</div>
                  <div class="text-sm font-bold">They don't know yet</div>
                </div>
              </div>
            </div>

            <div class="story-stage story-hidden absolute inset-0 flex items-center justify-center" data-stage="2">
              <div class="w-3/5 space-y-2">
                <div class="text-xs font-bold tracking-[0.18em] mb-2 text-center" style={`color: ${deep}`}>Guess their mission</div>
                <div class="bg-white rounded-xl border border-neutral-200 p-3 text-sm font-medium opacity-60">Hug first</div>
                <div class="rounded-xl p-3 text-sm font-bold border-2" style={`border-color: ${accent}; background-color: ${accent}11;`}>
                  Hold hands first ✓
                </div>
                <div class="bg-white rounded-xl border border-neutral-200 p-3 text-sm font-medium opacity-60">Say I love you first</div>
                <div class="bg-white rounded-xl border border-neutral-200 p-3 text-sm font-medium opacity-60">Take a photo first</div>
              </div>
            </div>

            <div class="story-stage story-hidden absolute inset-0 flex items-center justify-center" data-stage="3">
              <div class="relative w-1/2 aspect-square rounded-full flex items-center justify-center" style={`background-color: ${deep}; border: 2px solid ${accent}55;`}>
                <svg viewBox="0 0 100 100" class="w-1/2 h-1/2" style={`color: ${yellow}`}>
                  <circle cx="50" cy="50" r="42" fill="none" stroke="currentColor" stroke-width="3" opacity="0.5"/>
                  <line x1="50" y1="50" x2="50" y2="12" stroke="currentColor" stroke-width="4" stroke-linecap="round"/>
                  <line x1="50" y1="50" x2="50" y2="14" stroke="currentColor" stroke-width="4" stroke-linecap="round"/>
                  <circle cx="50" cy="50" r="3" fill="currentColor"/>
                </svg>
                <div class="absolute -bottom-8 left-1/2 -translate-x-1/2 text-xs font-bold tracking-[0.18em]" style={`color: ${deep}`}>
                  00:00 · Midnight
                </div>
              </div>
            </div>

            <div class="story-stage story-hidden absolute inset-0 flex items-center justify-center" data-stage="4">
              <div class="relative w-3/5">
                <div class="bg-white rounded-2xl border border-neutral-200 p-6 shadow-lg text-center">
                  <div class="inline-block px-3 py-1 rounded-md text-[10px] font-black tracking-[0.18em] mb-3 text-white" style={`background-color: ${accent}`}>SUCCESS</div>
                  <div class="text-5xl mb-3">💞</div>
                  <div class="text-base font-black tracking-tight mb-1">Both succeeded</div>
                  <div class="text-xs font-bold" style={`color: ${deep}`}>+100 cosmic points</div>
                </div>
              </div>
            </div>

            <div class="absolute -bottom-2 left-1/2 -translate-x-1/2 flex gap-1.5">
              {Array.from({ length: 5 }).map(() => (
                <span class="story-dot w-1.5 h-1.5 rounded-full bg-neutral-300"></span>
              ))}
            </div>
          </div>

          <div class="relative min-h-[12rem]">
            <div class="story-stage story-active absolute inset-0" data-stage="0">
              <p class="text-xs uppercase tracking-[0.18em] font-medium mb-3" style={`color: ${accent}`}>9:00 PM</p>
              <h3 class="text-3xl md:text-5xl font-black tracking-tight leading-[1.1] mb-4">
                A mission<br/>arrives.
              </h3>
              <p class="text-base text-[--color-muted] leading-relaxed">
                Every night at 9 PM, one card lands in your spaceship. Accept within the hour.
              </p>
            </div>
            <div class="story-stage story-hidden absolute inset-0" data-stage="1">
              <p class="text-xs uppercase tracking-[0.18em] font-medium mb-3" style={`color: ${deep}`}>Secret</p>
              <h3 class="text-3xl md:text-5xl font-black tracking-tight leading-[1.1] mb-4">
                Carry it out<br/>in secret.
              </h3>
              <p class="text-base text-[--color-muted] leading-relaxed">
                Your mission is hidden from them. They only see their own.
              </p>
            </div>
            <div class="story-stage story-hidden absolute inset-0" data-stage="2">
              <p class="text-xs uppercase tracking-[0.18em] font-medium mb-3" style={`color: ${accent}`}>Guess</p>
              <h3 class="text-3xl md:text-5xl font-black tracking-tight leading-[1.1] mb-4">
                Guess<br/>each other's.
              </h3>
              <p class="text-base text-[--color-muted] leading-relaxed">
                Pick one of four. Right answer = +100. Wrong = one more clue unlocks.
              </p>
            </div>
            <div class="story-stage story-hidden absolute inset-0" data-stage="3">
              <p class="text-xs uppercase tracking-[0.18em] font-medium mb-3" style={`color: ${deep}`}>Midnight</p>
              <h3 class="text-3xl md:text-5xl font-black tracking-tight leading-[1.1] mb-4">
                At 00:00,<br/>graded.
              </h3>
              <p class="text-base text-[--color-muted] leading-relaxed">
                When the day closes, today's mission is judged. Guesses unlock too.
              </p>
            </div>
            <div class="story-stage story-hidden absolute inset-0" data-stage="4">
              <p class="text-xs uppercase tracking-[0.18em] font-medium mb-3" style={`color: ${accent}`}>Reveal</p>
              <h3 class="text-3xl md:text-5xl font-black tracking-tight leading-[1.1] mb-4">
                See the result,<br/>together.
              </h3>
              <p class="text-base text-[--color-muted] leading-relaxed">
                Succeed = points + new room decor. Miss = a tiny, gentle cosmic forfeit.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- How to use -->
  <section class="max-w-6xl mx-auto w-full px-6 py-20 border-t border-[--color-border]">
    <p class="text-xs uppercase tracking-[0.18em] text-[--color-muted] font-medium mb-3 reveal">
      Flow
    </p>
    <h2 class="text-2xl md:text-4xl font-black tracking-tight leading-tight mb-12 reveal" data-delay="100">
      Here's how the first day goes.
    </h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-4">
      {steps.map((s, i) => (
        <article class="lift-luvit reveal flex flex-col" data-delay={String((i + 1) * 80)}>
          <div class="mb-4 relative">
            <div class="step-tilt step-tilt-luvit aspect-[9/19.5] rounded-[28px] overflow-hidden border border-neutral-200 bg-neutral-950 shadow-sm">
              <img src={s.image} alt={s.title} class="w-full h-full object-cover" loading="lazy" />
            </div>
            <span class="absolute -top-2 -left-2 w-8 h-8 rounded-full flex items-center justify-center text-xs font-black text-white shadow-md z-10" style={`background-color: ${accent}`}>
              {i + 1}
            </span>
          </div>
          <h3 class="text-sm font-bold tracking-tight mb-2 px-1">
            {s.title}
          </h3>
          <p class="text-xs text-[--color-muted] leading-relaxed px-1">
            {s.body}
          </p>
        </article>
      ))}
    </div>
  </section>

  <!-- Moments marquee -->
  <section class="py-20 border-t border-[--color-border] overflow-hidden">
    <div class="max-w-5xl mx-auto px-6">
      <p class="text-xs uppercase tracking-[0.18em] text-[--color-muted] font-medium mb-3 reveal">
        Tiny moments
      </p>
      <h2 class="text-2xl md:text-4xl font-black tracking-tight leading-tight mb-2 reveal" data-delay="100">
        Small things that build up.
      </h2>
      <p class="text-sm text-[--color-muted] mb-10 reveal" data-delay="200">
        The tiny, kind things that only happen inside Luvit.
      </p>
    </div>
    <div class="marquee-track">
      {[...moments, ...moments].map((m) => (
        <div class="flex items-center gap-3 px-6 py-3 mr-3 rounded-full border border-[--color-border] bg-white whitespace-nowrap">
          <span class="w-2 h-2 rounded-full" style={`background-color: ${accent}`}></span>
          <span class="text-base font-bold tracking-tight">{m}</span>
        </div>
      ))}
    </div>
  </section>

  <!-- FAQ -->
  <section class="max-w-5xl mx-auto w-full px-6 py-20 border-t border-[--color-border]">
    <p class="text-xs uppercase tracking-[0.18em] text-[--color-muted] font-medium mb-3 reveal">
      FAQ
    </p>
    <h2 class="text-2xl md:text-4xl font-black tracking-tight leading-tight mb-12 reveal" data-delay="100">
      Before you start.
    </h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      {faq.map((item, i) => (
        <article class="lift-luvit reveal bg-white rounded-2xl border border-[--color-border] p-8" data-delay={String((i + 1) * 100)}>
          <h3 class="text-base font-bold tracking-tight mb-3">
            {item.q}
          </h3>
          <p class="text-sm text-[--color-muted] leading-relaxed">
            {item.a}
          </p>
        </article>
      ))}
    </div>
  </section>

  <!-- Final CTA -->
  <section class="max-w-5xl mx-auto w-full px-6 py-24 border-t border-[--color-border]">
    <div class="reveal rounded-3xl p-10 md:p-16 text-center" style={`background: linear-gradient(135deg, ${accent}, ${deep});`}>
      <p class="text-xs uppercase tracking-[0.18em] text-white/70 font-medium mb-4">
        Now on the App Store
      </p>
      <h2 class="text-3xl md:text-5xl font-black tracking-tight text-white leading-[1.15] mb-6">
        Want tonight's<br/>first mission?
      </h2>
      <p class="text-white/80 text-base md:text-lg max-w-xl mx-auto mb-8 leading-relaxed">
        Both of you sign up, and it begins at 9 PM tonight. Free.
      </p>
      <a href={appStoreUrl} target="_blank" rel="noopener" class="magnet-cta cta-glow-luvit inline-flex items-center transition-transform hover:scale-105" aria-label="Download on the App Store">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 170 50" class="h-14 md:h-16 w-auto" role="img" aria-hidden="true">
          <rect width="170" height="50" rx="9" fill="#000"/>
          <path d="M30.5 26.6c0-3 2.5-4.5 2.6-4.5-1.4-2-3.6-2.3-4.4-2.3-1.9-.2-3.6 1.1-4.6 1.1-1 0-2.4-1.1-4-1-2 0-3.9 1.2-5 3-2.1 3.7-.5 9.1 1.5 12.1 1 1.5 2.2 3.1 3.8 3 1.5-.1 2.1-1 3.9-1 1.8 0 2.4 1 4 1 1.7 0 2.7-1.5 3.7-2.9 1.2-1.7 1.7-3.3 1.7-3.4-.1-.1-3.2-1.2-3.2-4.9zm-3-9c.8-1 1.4-2.4 1.3-3.8-1.2.1-2.7.8-3.6 1.8-.8.9-1.5 2.4-1.3 3.7 1.4.1 2.8-.7 3.6-1.7z" fill="#fff"/>
          <text x="46" y="20" fill="#fff" font-size="8" font-family="-apple-system, BlinkMacSystemFont, Helvetica, sans-serif" letter-spacing="0.3">Download on the</text>
          <text x="46" y="38" fill="#fff" font-size="17" font-weight="600" font-family="-apple-system, BlinkMacSystemFont, Helvetica, sans-serif" letter-spacing="-0.4">App Store</text>
        </svg>
      </a>
    </div>
  </section>

  <!-- Back -->
  <section class="max-w-5xl mx-auto w-full px-6 pb-16">
    <div class="flex flex-wrap gap-6 reveal">
      <a href="/en/" class="text-sm font-medium text-[--color-muted] hover:text-[--color-fg] transition-colors inline-flex items-center min-h-[44px]">
        ← East Noise home
      </a>
    </div>
  </section>

  <style is:global>
    .bg-luvit-ambient {
      background:
        radial-gradient(900px 500px at 80% 10%, #FF6B9D14, transparent 60%),
        radial-gradient(800px 500px at 10% 80%, #4B2E8314, transparent 60%),
        linear-gradient(180deg, #FFF6F8 0%, #FAFAF8 100%);
    }
    .pulse-dot { animation: pulse-dot 2.2s ease-in-out infinite; }
    @keyframes pulse-dot {
      0%, 100% { opacity: 0.6; transform: scale(1); }
      50% { opacity: 1; transform: scale(1.3); box-shadow: 0 0 0 6px rgba(255, 107, 157, 0.15); }
    }
    .reveal {
      opacity: 0;
      transform: translateY(24px);
      transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1), transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
      transition-delay: calc(var(--reveal-delay, 0) * 1ms);
    }
    .reveal.is-in { opacity: 1; transform: translateY(0); }
    .petal-fall-1 { animation: petal-fall 16s linear infinite; }
    .petal-fall-2 { animation: petal-fall 22s linear infinite; }
    .petal-fall-3 { animation: petal-fall 19s linear infinite; }
    @keyframes petal-fall {
      0% { transform: translateY(-20px) rotate(0deg); opacity: 0; }
      8% { opacity: 1; }
      92% { opacity: 1; }
      100% { transform: translateY(110vh) rotate(360deg); opacity: 0; }
    }
    .float-1 { animation: float-1 5s ease-in-out infinite; }
    .float-2 { animation: float-2 6.5s ease-in-out infinite 1s; }
    .float-3 { animation: float-3 4.5s ease-in-out infinite 0.4s; }
    @keyframes float-1 { 0%,100% { transform: translate(0,0); } 50% { transform: translate(6px,-10px); } }
    @keyframes float-2 { 0%,100% { transform: translate(0,0); } 50% { transform: translate(-8px,8px); } }
    @keyframes float-3 { 0%,100% { transform: translate(0,0); } 50% { transform: translate(4px,-6px); } }
    .bunny-left { animation: bunny-in 0.9s cubic-bezier(0.16, 1, 0.3, 1) 0.4s both, bunny-bob-left 4s ease-in-out infinite 1.5s; transform-origin: bottom center; }
    .bunny-right { animation: bunny-in 0.9s cubic-bezier(0.16, 1, 0.3, 1) 0.55s both, bunny-bob-right 4.5s ease-in-out infinite 1.7s; transform-origin: bottom center; }
    @keyframes bunny-in {
      0% { transform: translateY(40px) scale(0.7); opacity: 0; }
      60% { transform: translateY(-6px) scale(1.04); opacity: 1; }
      100% { transform: translateY(0) scale(1); opacity: 1; }
    }
    @keyframes bunny-bob-left { 0%,100% { transform: translateY(0) rotate(0deg); } 50% { transform: translateY(-6px) rotate(-2deg); } }
    @keyframes bunny-bob-right { 0%,100% { transform: translateY(0) rotate(0deg); } 50% { transform: translateY(-8px) rotate(2deg); } }
    .bubble-pop { animation: bubble-pop 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) 1.3s both; transform-origin: bottom; }
    @keyframes bubble-pop {
      0% { transform: scale(0); opacity: 0; }
      80% { transform: scale(1.05); opacity: 1; }
      100% { transform: scale(1); opacity: 1; }
    }
    .lift-luvit {
      transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    }
    .lift-luvit:hover {
      transform: translateY(-6px);
      box-shadow: 0 14px 30px rgba(255, 107, 157, 0.12);
    }
    .story-stage {
      transition: opacity 0.5s cubic-bezier(0.16, 1, 0.3, 1), transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    }
    .story-active { opacity: 1; transform: translateY(0) scale(1); }
    .story-hidden { opacity: 0; transform: translateY(20px) scale(0.96); pointer-events: none; }
    .marquee-track {
      display: flex;
      align-items: center;
      animation: marquee-scroll 38s linear infinite;
      width: max-content;
    }
    @keyframes marquee-scroll {
      from { transform: translateX(0); }
      to   { transform: translateX(-50%); }
    }
    .step-tilt-luvit { transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1); }
    .lift-luvit:hover .step-tilt-luvit { transform: rotate(-1.5deg) translateY(-4px); }
    .tilt-3d { transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1); transform-style: preserve-3d; }
    .cta-glow-luvit { box-shadow: 0 12px 30px rgba(255, 107, 157, 0.4); }
    .magnet-cta { transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1); }
    @media (prefers-reduced-motion: reduce) {
      .reveal { opacity: 1; transform: none; transition: none; }
      .petal-fall-1, .petal-fall-2, .petal-fall-3,
      .float-1, .float-2, .float-3,
      .marquee-track, .pulse-dot { animation: none; }
    }
  </style>

  <script is:inline>
    (() => {
      if (typeof window === 'undefined') return;
      const targets = document.querySelectorAll('.reveal');
      if ('IntersectionObserver' in window) {
        const io = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              const delay = entry.target.getAttribute('data-delay');
              if (delay) entry.target.style.setProperty('--reveal-delay', delay);
              entry.target.classList.add('is-in');
              io.unobserve(entry.target);
            }
          });
        }, { rootMargin: '0px 0px -10% 0px', threshold: 0.1 });
        targets.forEach((el) => io.observe(el));
      } else {
        targets.forEach((el) => el.classList.add('is-in'));
      }

      const story = document.getElementById('story');
      if (story) {
        const stages = story.querySelectorAll('.story-stage');
        const dots = story.querySelectorAll('.story-dot');
        const stageCount = 5;
        const updateStory = () => {
          const rect = story.getBoundingClientRect();
          const totalScroll = story.offsetHeight - window.innerHeight;
          const scrolled = Math.min(Math.max(-rect.top, 0), totalScroll);
          const progress = totalScroll > 0 ? scrolled / totalScroll : 0;
          const activeIdx = Math.min(Math.floor(progress * stageCount), stageCount - 1);
          stages.forEach((el) => {
            const idx = parseInt(el.getAttribute('data-stage') || '0', 10);
            if (idx === activeIdx) {
              el.classList.add('story-active');
              el.classList.remove('story-hidden');
            } else {
              el.classList.remove('story-active');
              el.classList.add('story-hidden');
            }
          });
          dots.forEach((dot, i) => {
            if (i <= activeIdx) {
              dot.style.backgroundColor = '#FF6B9D';
              dot.classList.remove('bg-neutral-300');
            } else {
              dot.style.backgroundColor = '';
              dot.classList.add('bg-neutral-300');
            }
          });
        };
        let raf = 0;
        const onScroll = () => {
          if (raf) return;
          raf = requestAnimationFrame(() => { raf = 0; updateStory(); });
        };
        window.addEventListener('scroll', onScroll, { passive: true });
        window.addEventListener('resize', onScroll);
        updateStory();
      }

      const tiltEl = document.getElementById('hero-tilt');
      if (tiltEl && window.matchMedia('(pointer: fine)').matches) {
        const parent = tiltEl.parentElement;
        parent.addEventListener('mousemove', (e) => {
          const rect = tiltEl.getBoundingClientRect();
          const cx = rect.left + rect.width / 2;
          const cy = rect.top + rect.height / 2;
          const dx = (e.clientX - cx) / rect.width;
          const dy = (e.clientY - cy) / rect.height;
          const ry = dx * 6;
          const rx = -dy * 4;
          tiltEl.style.transform = `perspective(900px) rotateX(${rx}deg) rotateY(${ry}deg)`;
        });
        parent.addEventListener('mouseleave', () => { tiltEl.style.transform = ''; });
      }

      document.querySelectorAll('.magnet-cta').forEach((btn) => {
        if (!window.matchMedia('(pointer: fine)').matches) return;
        btn.addEventListener('mousemove', (e) => {
          const rect = btn.getBoundingClientRect();
          const x = e.clientX - (rect.left + rect.width / 2);
          const y = e.clientY - (rect.top + rect.height / 2);
          btn.style.transform = `translate(${x * 0.25}px, ${y * 0.25}px)`;
        });
        btn.addEventListener('mouseleave', () => { btn.style.transform = ''; });
      });
    })();
  </script>
</Layout>
